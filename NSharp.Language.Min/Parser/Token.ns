use NSharp.Core

ns NSharp.Language.Min

public class Token
    public fn Type TokenType
    public fn Position Position
    public fn Value str

    public fn new(type TokenType, position Position, value str)
        Type = type
        Position = position
        Value = value

    public over fn ToString() str is "{Type} {Position} '{Value}'"
