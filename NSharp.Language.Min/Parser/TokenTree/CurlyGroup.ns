use NSharp.Core

ns NSharp.Language.Min.TokenTree

public class CurlyGroup is Group
    public fn new(position Position, parent ?Node) base(position, parent) is ; empty

    public over fn Name str is "{{group}}"
