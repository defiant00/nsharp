use NSharp.Core

ns NSharp.Language.Min.TokenTree

public class ParenthesisGroup is Group
    public fn new(position Position, parent ?Node) base(position, TokenType.ParenthesisGroup, parent) is ; empty

    public over fn Name str is "(group)"
