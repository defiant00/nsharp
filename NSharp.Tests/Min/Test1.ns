ns NSharp.Tests.Min

class SealedClass
    ; empty
open class OpenClass
    open fn Open() is ; empty
    open override fn OpenOverride() is ; empty
    override fn Override() is ; empty

abstract class AbstractClass
    ; empty