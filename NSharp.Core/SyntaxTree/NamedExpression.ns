ns NSharp.Core.SyntaxTree

public class NamedExpression is Node
    public fn Value Node
    public fn Name Node

    public fn new(position Position, value Node, name Node) base(position)
        Value = value
        Name = name
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
