ns NSharp.Core.SyntaxTree

public class UnaryOperator is Node
    public fn Operator Node
    public fn Value Node

    public fn new(position Position, op Node, value Node) base(position)
        Operator = op
        Value = value
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
