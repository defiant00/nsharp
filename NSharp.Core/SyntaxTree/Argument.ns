ns NSharp.Core.SyntaxTree

public class Argument is Node
    public fn Name ?IdentifierToken
    public fn Modifiers List{ArgumentModifierToken}
    public fn Value Expression

    public fn new(position Position, name ?IdentifierToken, modifiers List{ArgumentModifierToken}, value Expression) base(position)
        Name = name
        Modifiers = modifiers
        Value = value
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
