ns NSharp.Core.SyntaxTree

public class ImportToken is Statement
    public fn NameParts List{str}

    public fn new(position Position, nameParts ?List{str} = null) base(position) is NameParts = nameParts ?? new()
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
