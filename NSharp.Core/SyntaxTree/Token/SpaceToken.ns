ns NSharp.Core.SyntaxTree

public class SpaceToken is Node
    public fn Size int

    public fn new(position Position, size int) base(position) is Size = size
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
