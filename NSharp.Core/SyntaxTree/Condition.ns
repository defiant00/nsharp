ns NSharp.Core.SyntaxTree

public class Condition is Node
    public fn Value Node
    public fn Result Node

    public fn new(position Position, value Node, result Node) base(position)
        Value = value
        Result = result
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
