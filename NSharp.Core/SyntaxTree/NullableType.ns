ns NSharp.Core.SyntaxTree

public class NullableType is Node
    public fn Type Node

    public fn new(position Position, type Node) base(position) is Type = type
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
