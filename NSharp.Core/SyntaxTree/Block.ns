ns NSharp.Core.SyntaxTree

public class Block is Statement
    public fn Statements List{Statement}

    public fn new(position Position, statements ?List{Statement} = null) base(position) is Statements = statements ?? new()
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
