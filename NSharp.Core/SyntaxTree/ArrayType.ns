ns NSharp.Core.SyntaxTree

public class ArrayType is Expression
    public fn Type Expression
    public fn Dimension int

    public fn new(position Position, type Expression, dimension int) base(position)
        Type = type
        Dimension = dimension
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
