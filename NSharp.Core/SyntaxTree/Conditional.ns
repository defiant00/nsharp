ns NSharp.Core.SyntaxTree

public class Conditional is Node
    public fn Value Node
    public fn Conditions List{Node} = new()

    public fn new(position Position, value Node) base(position) is Value = value
    
    public over fn Accept(visitor ISyntaxTreeVisitor) is visitor.Visit(this)
