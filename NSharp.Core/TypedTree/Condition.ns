ns NSharp.Core.TypedTree

public class Condition is Node
    public fn Value Expression
    public fn Result Expression

    public fn new(position Position, value Expression, result Expression) base(position)
        Value = value
        Result = result
    
    public over fn Accept(visitor ITypedTreeVisitor) is visitor.Visit(this)
